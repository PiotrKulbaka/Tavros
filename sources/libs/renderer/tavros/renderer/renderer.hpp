#pragma once

#include <tavros/core/memory/memory.hpp>
#include <tavros/renderer/rhi/graphics_device.hpp>

namespace tavros::renderer
{

    /*class renderer
    {
    public:
        renderer(void* native_window_handle);
        ~renderer();

        void set_render_target(void* native_window_handle);

        void resize(uint32 width, uint32 height);

    private:
        core::unique_ptr<rhi::graphics_device> m_device;
        rhi::frame_composer_handle             m_composer_h;
    };*/

} // namespace tavros::renderer
